/* eslint-disable arrow-body-style */

const Header = (props) => {
  return (
    `<header class=${props.class}>
            <img class="logo" src="../Imagens/header-logo.png">
            </header>
        `);
};

export default Header;
